{
  "tui": "fullscreen",
  "theme": "dark",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "dir=$(basename \"$PWD\"); if [ -n \"$TMUX_PANE\" ]; then win=$(tmux display-message -t \"$TMUX_PANE\" -p '#W' 2>/dev/null); fi; label=${win:+$win, }$dir; ~/.config/scripts/agent-notify-if-unfocused \"Claude Code\" \"Agent finished work in $label\" 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
